"use client";
import DemoCarousel from "@/components/Sub-Nav/Sub/Carousel";
import useProducts from "@/hooks/useProducts";

export default function Home() {
  // const { products } = useProducts();
  // console.log(products);
  return (
    <>
      <DemoCarousel />
    </>
  );
}
